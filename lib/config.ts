/**
 * Centralised configuration constants
 * All tuneable values live here — override via environment variables.
 */

export const DEFAULT_AZURE_TRANSCRIPT_MAX_CALLS_PER_DAY = 120
export const DEFAULT_AZURE_TRANSCRIPT_MAX_CALLS_PER_MONTH = 1500

/** Next.js route maxDuration (seconds) */
export const API_MAX_DURATION_SECONDS = 60
